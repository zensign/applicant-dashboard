import { TableFavoritedPipe } from './table-favorited.pipe';

describe('TableFavoritedPipe', () => {
  it('create an instance', () => {
    const pipe = new TableFavoritedPipe();
    expect(pipe).toBeTruthy();
  });
});
